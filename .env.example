# Copia este archivo como ".env" y completa tu token real.
# Nunca subas el archivo ".env" (con el token real) al repositorio.

# Personal Access Token de GitHub (classic o fine-grained) con permiso de lectura
# sobre repositorios públicos (repo:public_repo o solo lectura de contenidos).
GITHUB_TOKEN=
